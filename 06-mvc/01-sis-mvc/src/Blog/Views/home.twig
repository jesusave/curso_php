{% extends 'layout.twig' %}

{% block content %}
    <h5>Blog con PHP</h5>
    <p>Lista de articulos</p>

    <ul class="collection">
        {% for article in articles %}
            <li class="collection-item avatar">
                <i class="mdi-insert-comment circle"></i>
                <span class="title">{{ article.title }}</span>

                <p>{{ article.content }}</p>

                <a href="/article/{{ article.id }}">Leer màs</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}